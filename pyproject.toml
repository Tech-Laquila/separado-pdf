[project]
name = "separado-pdf"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.5",
    "uvicorn[standard]==0.32.1",
    "pypdf==5.1.0",
    "python-multipart==0.0.12",
    "httpx==0.27.2",
]

[tool.vercel]
entrypoint = "api:app"
